package edu.txstate.library.model;

public class ReferenceBook extends Book {

    public ReferenceBook(String author, String title, String publisher, String genre, float value, boolean isBestSeller) {
        super(author, title, publisher, genre, value, isBestSeller);
    }
}
